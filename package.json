{
  "name": "wedcms",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter core run start:dev",
    "build": "pnpm --filter core run build",
    "lint": "pnpm --filter core run lint",
    "test": "pnpm --filter core run test"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@nestjs/core",
      "argon2",
      "sharp",
      "unrs-resolver"
    ]
  }
}
